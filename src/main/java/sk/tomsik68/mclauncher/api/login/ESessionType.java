package sk.tomsik68.mclauncher.api.login;

/**
 * Type of session is used while login, so that login server can determine who it should log in.
 *
 * @author Tomsik68
 */
public enum ESessionType {
    LEGACY, MOJANG;
}
